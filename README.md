# Data Structure And Algo in Javascript

## Sorts

- Bubble sort && selection sort `best = average = worst = O(n ^ 2)`
- Quick sort `best = average = O(nlog(n)) worst= O(n^2)`
- Merge sort `best = average = worst = O(nlog(n))`
  
